{
  "fixture_id": "spec-04-clear-layout",
  "role": "architect",
  "must_emit_allowed_paths": {
    "non_halting": true,
    "every_component_has_allowed_paths": true,
    "excludes_harness_internals": [
      ".kstrl/",
      "pyproject.toml",
      "package.json",
      "kstrl/",
      ".github/"
    ],
    "includes_test_root_prefix": "tests/",
    "includes_feature_subtree": true
  },
  "description": "A clear, unambiguous spec that the architect should not halt on. Tests whether the v1.2.0 prompt's allowedPaths rules produce a sensible per-component scope: directories included (src + tests + feature subtree), harness internals excluded, no .kstrl/ or pyproject.toml leaks.",
  "expected_components": [
    {"id": "slugify", "expected_allowed_paths_contain": ["src/", "tests/", "scripts/kstrl/feature/slugify/"]}
  ]
}
